# 存储类型配置
# database: 使用 PostgreSQL 数据库（推荐用于生产环境）
# file: 使用文件存储（仅用于开发测试）
STORAGE_TYPE=database

# 数据库配置（使用 coze-coding-dev-sdk 时自动配置）
# DATABASE_URL=postgresql://user:password@host:port/database

# Next.js 配置
NODE_ENV=production
